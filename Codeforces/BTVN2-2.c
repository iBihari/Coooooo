#include <stdio.h>
#include <math.h>
int main()
{
    int c = 10, S, Sbm, V;
    S = pow(c, 2);
    printf("%d\n", S);
    Sbm = pow(c, 2) * 6;
    printf("%d\n", Sbm);
    V = c * c * c;
    printf("%d\n", V);
    return 0;
}   